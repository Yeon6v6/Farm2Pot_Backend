# USER DEV - backend server port
server:
  port: 18080

spring:
  datasource:
    url: jdbc:mysql://localhost:13306/farm2Pot
    username: farm2Pot
    password: farm2Pot
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: localhost
    port: 16379

kafka:
  bootstrap-servers: localhost:19092

# frontend server host
frontend:
  host: localhost

