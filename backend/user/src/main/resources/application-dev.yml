# USER DEV - backend server port
server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/farm2Pot
    username: farm2Pot
    password: farm2Pot
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: redis-master
    port: 6379

#kafka:
#  bootstrap-servers: kafka:9092

# frontend server host
frontend:
  host: localhost

